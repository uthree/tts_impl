[project]
name = "tts-impl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.4.1",
    "torchaudio>=2.4.1",
    "numpy<2",
    "gradio>=5.0.2",
    "pyworld>=0.3.4",
    "torchfcpe>=0.0.4",
    "lightning>=2.4.0",
    "tensorboard>=2.18.0",
    "tqdm>=4.66.5",
    "safetensors>=0.4.5",
    "alias-free-torch>=0.0.6",
    "hydra-core>=1.3.2",
    "pytest>=8.3.3",
    "mypy>=1.11.2",
    "numba>=0.60.0",
    "cython>=3.0.11",
    "pyopenjtalk-plus>=0.3.4.post5",
    "phonemizer>=3.3.0",
    "librosa>=0.10.2.post1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
pythonpath = ["src/"]

[tool.hatch.build.targets.wheel]
packages = ["src/"]
